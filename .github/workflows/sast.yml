name: Perform Mixeway Scan
jobs:
 mixeway-scan:
 name: Mixeway Scanning
 runs-on: ubuntu-latest
 steps:
 - uses: actions/checkout@v1
 - name: Prepare variables
 id: vars
 shell: bash
 run:  echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"; echo "::set-output name=sha_short::$(git rev-parse HEAD)"
 - name: Prepare Mixeway docker image
 run: docker pull mixeway/scanner:latest
 - name: Run Scan
 run:  docker run -e OSS_USERNAME=omegalul927@gmail.com -e OSS_KEY=b91c9adc92d603dcc50ab4881b4982b3357bf860 -e MIXEWAY_URL=https://hub.mixeway.io -e MIXEWAY_KEY=3c887286-6f80-4b4a-a283-0b51de1f92b9 -e MIXEWAY_PROJECT_ID=87 -e MODE=STANDALONE -e MIXEWAY_PROJECT_NAME=omegalul927a__lab_secure_app -e COMMIT_ID=test -e BRANCH=master -v /opt/lab_sec_audit/sast:/opt/sources mixeway/scanner:latest
